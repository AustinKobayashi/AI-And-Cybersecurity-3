# Models

This folder is reserved for future trained model artefacts and model metadata.

The planned baseline model is an explainable decision tree or random forest classifier trained on simulated events.

Model records should eventually include:

- Model name.
- Model version.
- Training dataset hash.
- Feature list.
- Label list.
- Evaluation metrics.
- SHA-256 hash of the saved model artefact.

